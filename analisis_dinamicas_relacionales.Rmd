Row
-----
### <b style="font-size: 22px;">Relación entre Conflictos y la Putuación de adicción</b>

Se observa una tendencia positiva, puesto que a medida que aumentan las puntuaciones de adicción obtenidas de los estudiantes, también tienden a aumentar los conflictos reportados por el uso de la red social. La línea de regresión en color rojo refuerza esta asociación, indicando que los estudiantes con niveles más altos de adicción suelen experimentar más tensiones sociales relacionados con su uso, lo cual sugiere que la adicción a las redes sociales podría ser un factor que afecta en las relaciones interpersonales.


```{r}
library(ggplot2)
ggplot(
  data = datos_estudiantes,
  aes(x = Addicted_Score,
      y = Conflicts_Over_Social_Media)
) +
  geom_point() + 
  geom_smooth(
    method = "lm",
    se = FALSE,
    color = "red",
    linewidth = 1
  ) +
  labs(
    x = "Puntuación de adicción",
    y = "Conflictos de la red social"
  )

```




Row
--------------------------------------------------------------------
### <b style="font-size: 22px;">Matriz de Correlación Cuantitativa</b>

La matriz de correlaciones revela tres relaciones clave: primero, existe una correlación negativa muy fuerte entre el nivel de adicción y la salud mental (r = -0.95), lo que indica que a mayor adicción, peor es el estado de salud mental. Segundo, el nivel de adicción también muestra una correlación positiva muy alta con los conflictos generados por el uso de redes (r = 0.93), evidenciando que un uso más problemático se asocia con mayores tensiones interpersonales. Por último, el uso diario de redes presenta una correlación positiva fuerte con el nivel de adicción (r = 0.83), lo que sugiere que pasar más horas conectado incrementa significativamente el riesgo de comportamientos adictivos.

<div style="position: relative;">

```{r cargar-correlaciones, echo=FALSE, message=FALSE, warning=FALSE}
datos_estudiantes <- read.csv("datos_estudiantes.csv")

# Libreria necesaria para la elaboración de la matriz de correlación
library(corrplot)

# Calculamos la matriz de correlación únicamente con las variables numéricas todas estas cuantitivas
# relevantes para el análisis (uso diario del celular, horas de sueño, salud mental, 
# conflictos por redes sociales y nivel de adicción). La opción "complete.obs" asegura 
# que solo se usen observaciones completas, es decir, casos sin datos faltantes. 
# Luego, visualizamos la matriz de correlación utilizando corrplot con el método "number",
# lo que permite ver directamente los valores numéricos de cada correlación.
# Seleccionamos las variables de interés y creamos la matriz de correlación
# Luego renombramos las columnas para que aparezcan en español en el gráfico.
x <- cor(datos_estudiantes[, c(
  "Avg_Daily_Usage_Hours",
  "Sleep_Hours_Per_Night",
  "Mental_Health_Score",
  "Conflicts_Over_Social_Media",
  "Addicted_Score"
)], use = "complete.obs")

# Renombramos las variables para que corrplot muestre los nombres en español
colnames(x) <- c(
  "Uso diario (horas)",
  "Horas de sueño",
  "Salud mental",
  "Conflictos por redes",
  "Nivel de adicción"
)

rownames(x) <- colnames(x)  # también para las filas

# Graficamos la matriz mostrando los valores numéricos de cada correlación
corrplot(x, method = "number")

```
</div>

